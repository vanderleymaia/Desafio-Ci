steps:

# - id: "Realizando docker-compose up"
#   name: 'docker/compose'
#   args: ['up', '-d']

- id: Run test
  name: "gcr.io/cloud-builders/go"
  args: ["test","./src"]
  env: ["GOPATH=."]

- id: Run the program
  name: "gcr.io/cloud-builders/go"
  args: ["run", "./src"]
  env: ["GOPATH=."]

- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--build-arg'
  - 'version=1.24.0'
  - '-t'
  - 'gcr.io/$PROJECT_ID/docker-compose:latest'
  - '-t'
  - 'gcr.io/$PROJECT_ID/docker-compose:1.24.0'
  - '.'
- name: 'gcr.io/$PROJECT_ID/docker-compose'
  args: ['version']
images:
  - 'gcr.io/$PROJECT_ID/docker-compose:latest'
  - 'gcr.io/$PROJECT_ID/docker-compose:1.24.0'