steps:
- id: Run test
  name: "gcr.io/cloud-builders/go"
  args: ["test"]
  # env: ["GOPATH=."]

- id: Run the program
  name: "gcr.io/cloud-builders/go"
  args: ["run","main.go"]
  # env: ["GOPATH=."]

